{
  "name": "letto-ai",
  "version": "0.2.0",
  "description": "LETTO.LIVE — AI kurator putničkih dealova za Balkan",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "vercel --prod",
    "build:seo": "node scripts/generate-sitemap.mjs && node scripts/generate-destination-pages.mjs",
    "build:sitemap": "node scripts/generate-sitemap.mjs",
    "build:destinations": "node scripts/generate-destination-pages.mjs"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.19.0",
    "@sentry/node": "^10.52.0",
    "firebase-admin": "^13.8.0",
    "google-auth-library": "^9.0.0",
    "pdfkit": "^0.18.0",
    "stripe": "^17.4.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
