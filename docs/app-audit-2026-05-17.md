# LETTO.LIVE · App Audit · 2026-05-17

Audit posle Commit 1 polish faze (`feat/polish-hero` @ c0eeeee).
Restore point: `v1.0-soft-launch-ready` (73e6807).

## A. Arhitektura
AI travel-deal kurator. Static `public/` HTML + Vercel serverless `api/` (Node 20 ESM,
bez build koraka — `vercel.json` buildCommand `"true"`). Firebase/Firestore projekat
`letto-ai`, Stripe (subscription mode), Resend (email), Telegram (bot + 2 kanala).
**12 HTML stranica · 19 API ruta · 9 lib modula.**

## B. Stranice
| Page | Lines | Napomena |
|---|---|---|
| index.html | 6077 | Monolit — inline CSS+JS. Hero redizajniran (Commit 1). |
| results.html | 7521 | Monolit. Mix builder (Stage 1/2/3). Najveći fajl. |
| me.html | 394 | **Van design-token sistema** — svoj `:root`, svoj styling. |
| trip.html | 479 | Svoj manji `:root` (drugačija paleta). Envelope stranica. |
| about/admin/dobrodosao/impressum/metrics/privacy/terms/admin-scraping | 130-1026 | Support. |

- `index.html` + `results.html` su monolitni (6k/7.5k linija, sve inline). Održavanje teško.
- `:root` token blok dupliran verbatim u index/results (i polish tokeni sad takođe). Nema deljenog CSS fajla.

## C. API rute (19)
- **Plaćanje:** stripe-checkout, stripe-go, stripe-webhook, customer-portal, recovery
- **Mix delivery:** save-mix, save-mix-premium, trip, me
- **Katalog:** packages, hotels-search, cj-refresh, scrape-status
- **Lead-ovi:** lead-capture, lead-confirm
- **Ops:** admin, health, notify-admin, telegram-webhook

Vercel function overrides: hotels-search 25s, cj-refresh 60s, admin 60s.

## D. Bezbednost
- ✅ `.secrets/`, `.env*` gitignored. Stripe webhook signature verifikacija prisutna.
  Rate limiting (`lib/rate-limit.js`) na save-mix/save-mix-premium. Nema secret leak-a u `console.log`.
- ✅ **FIXED u ovom audit-u:** `backups/` dodato u `.gitignore`. Sadrži
  `env.production.snapshot` + `restore-point-*.tar.gz` sa **živim secret-ima** — bilo je
  untracked, `git add -A` bi ih commit-ovao. Sad ignorisano (efektivno odmah).
  ⚠️ Fajlovi i dalje postoje lokalno — ne upload-uj `backups/` nešifrovano.
- `/trip/{tripId}` je javni bearer-link (po dizajnu).

## E. Poznati problemi / tech debt
| # | Prioritet | Problem |
|---|---|---|
| 1 | **P0** | **Katalog premium gating i dalje pokvaren.** `/api/packages` sessioned zahtev dobija CDN-keširan anon (scrub) odgovor (`x-vercel-cache: HIT`, `cache-control: public`). Header fix (73e6807) je nužan ali nedovoljan — treba `&auth=1` URL cache-bust follow-up (predloženo, NIJE urađeno). Premium user i dalje vidi zaključane flight fields na homepage katalogu. |
| 2 | P1 | `stripe-webhook.js:872 TODO` — otkazani premium user se NE izbacuje iz premium Telegram kanala; zadržava pristup. |
| 3 | P1 | Plausible `mix_finished` event se fire-uje iz results.html ali Plausible skripta nije učitana nigde → event je dormant. |
| 4 | P2 | Mrtav kod od hero restyle-a (polish branch): stari `.hero*`/`.solari-board` CSS (~34 selektora) + Solari JS (~130 linija) sad neiskorišćeni u index.html. Cleanup commit posle merge-a. |
| 5 | P2 | `toTripShape` dupliran u save-mix.js + save-mix-premium.js — ekstrahovati u `lib/mix-shape.js`. |
| 6 | P2 | `:root` token blok dupliran u index/results/trip — nema deljenog CSS fajla. |
| 7 | P3 | results.html short-form vs long-form param naming (`origin` vs `origin_iata`) — Hotfix-9 B6 komentar, nerešen refactor. |
| 8 | P3 | Nekonzistentni media-query breakpoint-ovi (760/768/600/900/1024/720/480/1180). |
| 9 | P3 | me.html van design-token sistema — poravnati pri Phase 2 polish-u. |
| 10 | P3 | Monolitni index.html (6077) / results.html (7521) — sve inline. Dugoročno razbiti. |

## F. polish-hero branch
`feat/polish-hero` @ `c0eeeee` — Phase 0-2 (tokeni + hero restyle + ticker). Preview
`letto-nrtik1rt9-miroslav-paunov.vercel.app` — build Ready, ali Vercel Deployment
Protection gejtuje URL (HTTP 401 bez Vercel login-a) → Miroslav verifikuje u browseru.
Phase za Commit 2/3 još nisu specifikovane.

## G. Preporuke (po prioritetu)
1. **P0 — popraviti katalog premium gating** (`&auth=1` cache-bust u index.html packages fetch). Premium korisnici trenutno ne dobijaju otključan katalog koji plaćaju.
2. **P0 — commit-ovati `.gitignore` izmenu** (`backups/`) na main, da zaštita bude trajna.
3. **P1** — stripe-webhook: izbaciti otkazane korisnike iz premium TG kanala (`banChatMember`).
4. **P1** — učitati Plausible skriptu ili ukloniti dormant `mix_finished` event.
5. **P2** — posle polish merge-a: obrisati mrtav hero/Solari kod.
6. **P2** — ekstrahovati `toTripShape` → `lib/mix-shape.js`; deljeni CSS token fajl.
7. **P3** — dugoročno: razbiti monolitne index/results.html.

## Stanje
App je funkcionalno zdrav za soft launch (e2e verifikovan: Mix delivery, dashboard,
email, Telegram). Glavni nedostatak je **P0 #1** — premium katalog gating ne radi do
kraja uprkos commit-u 73e6807. Sve ostalo je tech debt, ne blocker.
